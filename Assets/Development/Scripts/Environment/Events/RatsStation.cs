using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RatsStation : RandomEventBase
{
    [SerializeField] private GameObject rats;

   // make rats spawn into game , give them different spawning locations

   // make rats target customers , and use nav mesh to move to the customers
   // make rats eat and destroy costumers, give rats diferent states so they can hunt , run, kill, and die,
   // destroy and kill rats to end event, do a check to see if all rats are dead then end event.

}
